# Dépendances standalone — listées pour server.cfg
# (le vrai démarrage se fait dans server.cfg.example après 'exec dependencies.cfg')
# Installez les ressources dans resources/[standalone]/ puis décommentez.

# ensure oxmysql
# ensure ox_lib
# ensure ox_target
# ensure ox_inventory
